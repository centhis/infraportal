# Детальный план реализации UI управления настройками

## 1. Инфраструктура и навигация
*Общее описание: Задачи по подготовке фундамента для нового раздела: регистрация констант, настройка защищенных маршрутов и обновление бокового меню с учетом прав доступа.*

### 1.1. Регистрация констант и роутинг
*   1.1.1. (completed) Добавить константу `SETTINGS: '/settings'` в `src/shared/constants/routes.js`. Описание: Регистрация пути для нового раздела настроек.
*   1.1.2. (completed) Зарегистрировать маршрут в `src/app/routes/AppRoutes.jsx`. Описание: Добавление компонента `SettingsPage` в основной реестр маршрутов, обернутого в `ProtectedRoute` для обеспечения безопасности.

### 1.2. Обновление навигации
*   1.2.1. (completed) Добавить пункт "Настройки" в `settingsItems` в `src/components/layout/Navbar/Navbar.jsx`. Описание: Отображение нового раздела в боковом меню.
*   1.2.2. (completed) Применить компонент `Can` с пермишеном `settings:view` к пункту меню "Настройки". Описание: Ограничение видимости раздела в меню только для пользователей с соответствующими правами.
*   1.2.3. (completed) Настроить иконку `SettingsIcon` для нового пункта меню. Описание: Визуальная идентификация раздела настроек.

## 2. Слой API и управления состоянием
*Общее описание: Реализация взаимодействия с бэкендом и логики управления данными настроек на стороне клиента.*

### 2.1. API Сервисы
*   2.1.1. (completed) Создать `src/features/settings/api/settingsApi.js`. Описание: Реализация функций запросов к эндпоинтам бэкенда (`/settings/core` и `/settings/ldap`) с использованием настроенного Axios-клиента.
*   2.1.2. (completed) Реализовать метод `getCoreSettings`. Описание: Запрос списка базовых настроек системы.
*   2.1.3. (completed) Реализовать метод `updateCoreSetting`. Описание: Отправка изменений значения конкретной базовой настройки.
*   2.1.4. (completed) Реализовать метод `getLdapSettings`. Описание: Запрос всех параметров LDAP интеграции.
*   2.1.5. (completed) Реализовать метод `updateLdapSetting`. Описание: Отправка обновленных параметров LDAP.
*   2.1.6. (completed) Реализовать метод `testLdapConnection`. Описание: Вызов эндпоинта проверки LDAP-соединения с временными параметрами.
*   2.1.7. (completed) Реализовать метод `isLdapEnabled`. Описание: Легкий запрос для проверки статуса включения LDAP.

### 2.2. React Hooks
*   2.2.1. (completed) Создать раздельные хуки `useCoreSettings` и `useLdapSettings` в `src/features/settings/hooks/`. Описание: Разделение логики по принципу единственной ответственности для исключения избыточных запросов и конфликтов состояний.
*   2.2.2. (completed) Реализовать независимые состояния загрузки (loading) и ошибок для каждого хука. Описание: Улучшение UX за счет точной индикации статуса запроса для каждой группы настроек.
*   2.2.3. (completed) Реализовать функцию `fetchLdapEnabled` в `useLdapSettings`. Описание: Позволяет быстро проверить активность LDAP для адаптации интерфейса без загрузки всех настроек.

## 3. Компоненты интерфейса (Feature Settings)
*Общее описание: Создание визуальных компонентов для отображения и редактирования настроек, соблюдая DRY и SOLID.*

### 3.1. Основные страницы и вкладки
*   3.1.1. (completed) Создать `src/features/settings/pages/SettingsPage.jsx`. Описание: Главная страница раздела, выступающая контейнером для вкладок.
*   3.1.2. (completed) Создать `src/features/settings/components/SettingsTabs.jsx`. Описание: Компонент для переключения между "Core Settings" и "LDAP Integration" (по аналогии с `UserManagementTabs`).

### 3.2. Вкладка Core Settings (Базовые настройки)
*   3.2.1. (completed) Создать `src/features/settings/pages/CoreSettingsItemsPage.jsx`. Описание: Страница со списком настроек в виде карточек/списка.
*   3.2.2. (completed) Реализовать вертикальный список настроек с заголовками и описаниями. Описание: Улучшение читаемости и интуитивности.
*   3.2.3. (completed) Реализовать систему сохранения изменений (per-item или global). Описание: Удобный механизм обновления параметров.

### 3.3. Вкладка LDAP Settings (Настройки LDAP)
*   3.3.1. (completed) Создать `src/features/settings/pages/LdapSettingsItemsPage.jsx`. Описание: Страница настроек LDAP с группировкой параметров по секциям.
*   3.3.2. (completed) Реализовать логические группы (Connection, Users, Sync) для параметров LDAP. Описание: Улучшение навигации по множеству настроек.
*   3.3.3. (completed) Реализовать блок тестирования соединения с индикацией результата. Описание: Проверка корректности введенных данных.
*   3.3.4. (completed) Реализовать строгий режим черновика для «параметров связи и статуса» (`LDAP_ENABLED`, `LDAP_URI`, `LDAP_BIND_DN`, `LDAP_BIND_PASSWORD`, `LDAP_USER_FILTER`). Описание: Блокировка сохранения этих настроек до успешного прохождения теста сетевого соединения.
*   3.3.5. (completed) Реализовать независимое сохранение для расписания (`LDAP_SYNC_SCHEDULE`). Описание: Возврат кнопки сохранения в строку параметра, так как настройка не требует верификации сетевого соединения.
*   3.3.6. (completed) Реализовать клиентскую валидацию Cron-выражений для настройки расписания. Описание: Подсветка ошибок и блокировка сохранения при некорректном формате.

### 3.4. Общие компоненты и валидация
*   3.4.1. (completed) Создать универсальный компонент `SettingInputField`. Описание: Отрисовка input, checkbox или password-поля в зависимости от типа настройки (`type`).
*   3.4.2. (completed) Реализовать логику маскирования полей с `is_sensitive=True`. Описание: Показ плейсхолдера или звездочек для паролей, предотвращая утечку данных.
*   3.4.3. (completed) Настроить схемы валидации `zod` для форм. Описание: Проверка корректности введенных данных на стороне клиента.

## 4. Локализация (i18n)
*Общее описание: Подготовка текстового контента на русском языке для обеспечения многоязычности.*

*   4.1.1. (completed) Создать `src/i18n/locales/ru/settings.json`. Описание: Файл со всеми переводами для раздела настроек.
*   4.1.2. (completed) Интегрировать новый файл переводов в общую конфигурацию `i18n`. Описание: Активация локализации для фичи settings.

## 5. Тестирование и Качество
*Общее описание: Верификация работоспособности нового функционала.*

*   5.1.1. (completed) Написать unit-тесты для сервисов API (`settingsApi.test.js`). Описание: Проверка корректности формирования запросов.
*   5.1.2. (completed) Написать тесты для компонентов страниц с использованием `Vitest` и `React Testing Library`. Описание: Верификация рендеринга и взаимодействия пользователя с интерфейсом.
*   5.1.3. (completed) Протестировать работу разрешений (скрытие разделов/кнопок при отсутствии прав). Описание: Гарантия соблюдения RBAC. (Требует добавления ToastProvider в test-utils)

## 6. Документация
*Общее описание: Фиксация архитектурных решений и руководства пользователя.*

*   6.1.1. (completed) Создать `docs/frontend/settings.md`. Описание: Документация по структуре модуля настроек на фронтенде и способах его расширения.
*   6.1.2. (completed) Обновить `README.md` в корне фронтенда. Описание: Краткое описание новой функциональности.
