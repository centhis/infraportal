# Документация по API (v1)


Этот документ описывает все эндпоинты, доступные в **Public API v1**. 
Для описания служебных эндпоинтов, используемых воркерами, см. [Внутренний API](internal_api.md).

## Общие Принципы

-   **Версионирование**: Все маршруты API находятся под префиксом `/api/v1`.
-   **Аутентификация**: Большинство эндпоинтов требуют аутентификации с помощью JWT (JSON Web Token). Токен доступа (access token) должен быть передан в заголовке `Authorization` как `Bearer <token>`.
-   **Авторизация**: Доступ к ресурсам (пользователи, группы, роли) контролируется системой разрешений (permissions). Необходимые разрешения указаны для каждого эндпоинта.
-   **Пагинация**: Эндпоинты, возвращающие списки объектов (например, `GET /users/`, `GET /groups/`), поддерживают пагинацию с помощью query-параметров `skip` (смещение) и `limit` (количество элементов).

---

## Ресурс: Metrics (`/metrics`)

Эндпоинты для мониторинга состояния системы.

| Метод | Путь                 | Описание                                    | Аутентификация | Разрешения   |
| :---- | :------------------- | :------------------------------------------ | :------------- | :----------- |
| `GET`   | `/celery/status`     | Статус воркеров Celery (через Flower API).  | Обязательна    | -            |

---

## Ресурс: Auth (`/auth`)

Эндпоинты для аутентификации, управления сессиями и получения информации о текущем пользователе.

| Метод | Путь                 | Описание                                                              | Аутентификация | Разрешения |
| :---- | :------------------- | :-------------------------------------------------------------------- | :------------- | :--------- |
| `POST`  | `/login`             | Аутентифицирует пользователя по логину/паролю. Устанавливает `refresh_token` в httpOnly cookie. Возвращает `access_token` и список разрешений. | Не требуется    | -          |
| `GET`   | `/refresh`           | Обновляет `access_token`, используя `refresh_token` из cookie.            | Требуется cookie | -          |
| `GET`   | `/verify`            | Проверяет валидность текущего `access_token`.                          | Обязательна    | -          |
| `POST`  | `/logout`            | Завершает сессию пользователя, удаляя `refresh_token` из базы и cookie. | Требуется cookie | -          |
| `GET`   | `/me`                | Возвращает информацию о текущем аутентифицированном пользователе.     | Обязательна    | -          |
| `GET`   | `/sessions`          | Возвращает список активных сессий текущего пользователя.            | Обязательна    | -          |
| `DELETE`| `/sessions/{session_id}` | Завершает (отзывает) указанную сессию текущего пользователя.           | Обязательна    | -          |

---

## Ресурс: Users (`/users`)

Эндпоинты для управления пользователями.

| Метод | Путь                 | Описание                                    | Аутентификация | Разрешения   |
| :---- | :------------------- | :------------------------------------------ | :------------- | :----------- |
| `GET`   | `/`                  | Возвращает список пользователей с пагинацией. | Обязательна    | `users:view` |
| `POST`  | `/`                  | Создает нового пользователя.                | Обязательна    | `users:create` |
| `GET`   | `/{user_id}`         | Возвращает пользователя по его ID. Если запрашивается свой профиль, разрешение `users:view` не требуется. | Обязательна    | `users:view` (для чужих профилей) |
| `PUT`   | `/{user_id}`         | Обновляет данные пользователя по его ID.      | Обязательна    | `users:update` |
| `DELETE`| `/{user_id}`         | Удаляет пользователя по его ID.             | Обязательна    | `users:delete` |
| `GET`   | `/by-login/{login}`| Возвращает пользователя по его логину. Если запрашивается свой профиль, разрешение `users:view` не требуется. | Обязательна    | `users:view` (для чужих профилей) |
| `GET`   | `/{user_id}/permissions_report` | Возвращает отчет о разрешениях пользователя по его ID. | Обязательна    | `users:view` |

---

## Ресурс: Groups (`/groups`)

Эндпоинты для управления группами пользователей.

| Метод | Путь                 | Описание                                  | Аутентификация | Разрешения   |
| :---- | :------------------- | :---------------------------------------- | :------------- | :----------- |
| `GET`   | `/`                  | Возвращает список групп с пагинацией.     | Обязательна    | `users:view` |
| `POST`  | `/`                  | Создает новую группу.                     | Обязательна    | `users:create` |
| `GET`   | `/{group_id}`        | Возвращает группу по её ID.               | Обязательна    | `users:view` |
| `PUT`   | `/{group_id}`        | Обновляет данные группы по её ID.         | Обязательна    | `users:update` |
| `DELETE`| `/{group_id}`        | Удаляет группу по её ID.                  | Обязательна    | `users:delete` |

---

## Ресурс: Roles (`/roles`)

Эндпоинты для управления ролями.

| Метод | Путь                 | Описание                                                                     | Аутентификация | Разрешения   |
| :---- | :------------------- | :--------------------------------------------------------------------------- | :------------- | :----------- |
| `GET`   | `/`                  | Возвращает список ролей с пагинацией. Каждая роль включает список разрешений. | Обязательна    | `users:view` |
| `POST`  | `/`                  | Создает новую роль.                                                          | Обязательна    | `users:create` |
| `GET`   | `/{role_id}`         | Возвращает роль по её ID, включая список разрешений.                         | Обязательна    | `users:view` |
| `PUT`   | `/{role_id}`         | Обновляет данные роли по её ID (включая список ID разрешений).              | Обязательна    | `users:update` |
| `DELETE`| `/{role_id}`         | Удаляет роль по её ID.                                                       | Обязательна    | `users:delete` |

---

## Ресу-рс: Permissions (`/permissions`)

Эндпоинты для получения информации о разрешениях.

| Метод | Путь | Описание                               | Аутентификация | Разрешения   |
| :---- | :--- | :------------------------------------- | :------------- | :----------- |
| `GET`   | `/`  | Возвращает полный список всех доступных разрешений в системе. | Обязательна    | `users:view` |
2. 
3. ---
4. 
5. ## Ресурс: Tasks (`/tasks`)
6. 
7. Эндпоинты для управления фоновыми задачами.
8. 
9. | Метод | Путь                 | Описание                                  | Аутентификация | Разрешения   |
10. | :---- | :------------------- | :---------------------------------------- | :------------- | :----------- |
11. | `GET`   | `/tasks`             | Список доступных типов задач (определения). | Обязательна    | `tasks:read` |
12. | `POST`  | `/tasks/{name}/run`  | Запуск задачи по имени. Параметры в теле.   | Обязательна    | Зависит от задачи |
13. | `GET`   | `/task_executions`   | История запусков задач.                   | Обязательна    | `tasks:read` |
14. | `GET`   | `/task_executions/{id}` | Детали конкретного запуска.              | Обязательна    | `tasks:read` |
15. 
16. ## Ресурс: Settings (`/settings`)
17. 
18. Эндпоинты разделены по подмодулям.
19. 
20. ### Core Settings (`/settings/core`)
21. 
22. | Метод | Путь       | Описание                         | Разрешения       |
23. | :---- | :--------- | :------------------------------- | :--------------- |
24. | `GET`   | `/`        | Список всех базовых настроек.    | `settings:view`  |
25. | `GET`   | `/{key}`   | Получить одну настройку по ключу.| `settings:view`  |
26. | `PUT`   | `/{key}`   | Обновить значение настройки.     | `settings:update`|
27. 
28. ### LDAP Settings (`/settings/ldap`)
29. 
30. | Метод | Путь       | Описание                         | Разрешения       |
31. | :---- | :--------- | :------------------------------- | :--------------- |
32. | `GET`   | `/`        | Список всех LDAP настроек.       | `settings:view`  |
| `GET`   | `/is_enabled` | Проверка статуса интеграции.    | Любой (Auth)     |
| `PUT`   | `/{key}`   | Обновить LDAP настройку.         | `settings:update`|
| `POST`  | `/test-connection` | Тест подключения к LDAP.        | `settings:update`|
| `POST`  | `/debug-user` | Отладочный поиск пользователя. | `settings:update`|
36.
